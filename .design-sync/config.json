{
  "pkg": "autoblogr",
  "globalName": "AutoBlogr",
  "projectId": "c5f18500-9163-438a-930b-2187acff5371",
  "shape": "package",
  "srcDir": "src/components/ui",
  "tsconfig": "jsconfig.json",
  "cssEntry": ".design-sync/.cache/tailwind.css",
  "docsDir": ".design-sync/docs",
  "overrides": {
    "Dialog": {
      "cardMode": "single",
      "viewport": "720x460"
    },
    "AlertDialog": {
      "cardMode": "single"
    },
    "Sheet": {
      "cardMode": "single"
    },
    "Drawer": {
      "cardMode": "single"
    },
    "Popover": {
      "cardMode": "single"
    },
    "HoverCard": {
      "cardMode": "single"
    },
    "Tooltip": {
      "cardMode": "single"
    },
    "DropdownMenu": {
      "cardMode": "single"
    },
    "ContextMenu": {
      "cardMode": "single"
    },
    "Command": {
      "cardMode": "single"
    },
    "Menubar": {
      "cardMode": "single"
    },
    "Sidebar": {
      "cardMode": "column"
    },
    "SidebarInput": {
      "cardMode": "column"
    },
    "SidebarMenuSkeleton": {
      "cardMode": "column"
    },
    "ChartContainer": {
      "cardMode": "column"
    },
    "NavigationMenu": {
      "cardMode": "column"
    },
    "Pagination": {
      "cardMode": "column"
    },
    "PaginationEllipsis": {
      "cardMode": "column"
    },
    "Tabs": {
      "cardMode": "column"
    },
    "Toast": {
      "cardMode": "single",
      "primaryStory": "Default"
    },
    "Toaster": {
      "cardMode": "single",
      "primaryStory": "MountedWithQueue"
    },
    "Select": {
      "cardMode": "single",
      "primaryStory": "OpenMenu"
    }
  },
  "buildCmd": "npx tailwindcss -c .design-sync/tailwind.ds.config.cjs -i src/index.css -o .design-sync/.cache/tailwind.css",
  "dtsPropsFor": {
    "Accordion": "  /** Whether one item or multiple items can be open at once. */\n  type?: \"single\" | \"multiple\";\n  /** Controlled value: a string when type=\"single\", a string[] when type=\"multiple\". */\n  value?: string | string[];\n  /** Uncontrolled initial value: a string when type=\"single\", a string[] when type=\"multiple\". */\n  defaultValue?: string | string[];\n  onValueChange?: (value: string | string[]) => void;\n  /** When type=\"single\", allows closing the open item by clicking its trigger again. */\n  collapsible?: boolean;\n  disabled?: boolean;\n  dir?: \"ltr\" | \"rtl\";\n  orientation?: \"horizontal\" | \"vertical\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Accordion.Root. */\n  [key: string]: unknown;",
    "AccordionContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Accordion.Content. */\n  [key: string]: unknown;",
    "AccordionItem": "  /** Unique value identifying this item within the Accordion. */\n  value: string;\n  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Accordion.Item. */\n  [key: string]: unknown;",
    "AccordionTrigger": "  className?: string;\n  children?: React.ReactNode;\n  disabled?: boolean;\n  /** Remaining props pass through to the underlying Radix Accordion.Trigger. */\n  [key: string]: unknown;",
    "Alert": "  /** Visual style. */\n  variant?: \"default\" | \"destructive\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div role=\"alert\">. */\n  [key: string]: unknown;",
    "AlertDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "AlertDialog": "  /** Controlled open state. */\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Root. */\n  [key: string]: unknown;",
    "AlertDialogAction": "  className?: string;\n  children?: React.ReactNode;\n  onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Action, styled with the shared buttonVariants() (default button look). */\n  [key: string]: unknown;",
    "AlertDialogCancel": "  className?: string;\n  children?: React.ReactNode;\n  onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Cancel, styled with buttonVariants({ variant: \"outline\" }). */\n  [key: string]: unknown;",
    "AlertDialogContent": "  className?: string;\n  children?: React.ReactNode;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onOpenAutoFocus?: (event: Event) => void;\n  onCloseAutoFocus?: (event: Event) => void;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Content, which this component already wraps in AlertDialogPortal + AlertDialogOverlay. */\n  [key: string]: unknown;",
    "AlertDialogDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Description. */\n  [key: string]: unknown;",
    "AlertDialogFooter": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "AlertDialogHeader": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "AlertDialogOverlay": "  className?: string;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Overlay. */\n  [key: string]: unknown;",
    "AlertDialogPortal": "  children?: React.ReactNode;\n  /** Element to portal into; defaults to document.body. */\n  container?: HTMLElement | null;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Portal. */\n  [key: string]: unknown;",
    "AlertDialogTitle": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Title. */\n  [key: string]: unknown;",
    "AlertDialogTrigger": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix AlertDialog.Trigger. */\n  [key: string]: unknown;",
    "AlertTitle": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <h5>. */\n  [key: string]: unknown;",
    "AspectRatio": "  /** Desired width/height ratio, e.g. 16 / 9. */\n  ratio?: number;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix AspectRatio.Root. */\n  [key: string]: unknown;",
    "Avatar": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Avatar.Root. */\n  [key: string]: unknown;",
    "AvatarFallback": "  className?: string;\n  children?: React.ReactNode;\n  /** Delay in ms before rendering the fallback, to avoid a flash while the image is still loading. */\n  delayMs?: number;\n  /** Remaining props pass through to the underlying Radix Avatar.Fallback. */\n  [key: string]: unknown;",
    "AvatarImage": "  className?: string;\n  src?: string;\n  alt?: string;\n  onLoadingStatusChange?: (status: \"idle\" | \"loading\" | \"loaded\" | \"error\") => void;\n  /** Remaining props pass through to the underlying Radix Avatar.Image (renders an <img>). */\n  [key: string]: unknown;",
    "Badge": "  /** Visual style. */\n  variant?: \"default\" | \"secondary\" | \"destructive\" | \"outline\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "Breadcrumb": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <nav aria-label=\"breadcrumb\">. */\n  [key: string]: unknown;",
    "BreadcrumbEllipsis": "  className?: string;\n  /** Remaining props pass through to the underlying <span role=\"presentation\">. Renders a fixed MoreHorizontal icon; does not accept children. */\n  [key: string]: unknown;",
    "BreadcrumbItem": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <li>. */\n  [key: string]: unknown;",
    "BreadcrumbLink": "  /** Render the child element instead of an <a>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  href?: string;\n  /** Remaining props pass through to the underlying <a> (or the child element when asChild is true). */\n  [key: string]: unknown;",
    "BreadcrumbList": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <ol>. */\n  [key: string]: unknown;",
    "BreadcrumbPage": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <span aria-current=\"page\" aria-disabled=\"true\" role=\"link\">. */\n  [key: string]: unknown;",
    "BreadcrumbSeparator": "  /** Custom separator content; defaults to a ChevronRight icon when omitted. */\n  children?: React.ReactNode;\n  className?: string;\n  /** Remaining props pass through to the underlying <li role=\"presentation\">. */\n  [key: string]: unknown;",
    "Button": "  /** Visual style. */\n  variant?: \"default\" | \"destructive\" | \"outline\" | \"secondary\" | \"ghost\" | \"link\";\n  /** Control height/padding. `icon` renders a square icon-only button. */\n  size?: \"default\" | \"sm\" | \"lg\" | \"icon\";\n  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  disabled?: boolean;\n  type?: \"button\" | \"submit\" | \"reset\";\n  onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;\n  /** Remaining props pass through to the underlying <button>. */\n  [key: string]: unknown;",
    "Calendar": "  /** Calendar mode, referenced internally (`props.mode === \"range\"`) to adjust cell rounding; forwarded to react-day-picker's DayPicker. */\n  mode?: \"single\" | \"multiple\" | \"range\";\n  /**\n   * Per-part class name overrides merged onto the built-in Tailwind classes. Keys mirror\n   * DayPicker's parts: `months`, `month`, `caption`, `caption_label`, `nav`, `nav_button`,\n   * `nav_button_previous`, `nav_button_next`, `table`, `head_row`, `head_cell`, `row`, `cell`,\n   * `day`, `day_range_start`, `day_range_end`, `day_selected`, `day_today`, `day_outside`,\n   * `day_disabled`, `day_range_middle`, `day_hidden`.\n   */\n  classNames?: Record<string, string>;\n  /** Show days from adjacent months to fill out the grid. Defaults to true. */\n  showOutsideDays?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying <DayPicker />. */\n  [key: string]: unknown;",
    "Card": "  /** Styled <div> wrapper (rounded border, background, shadow) used as a container for header/content/footer sections. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "CardContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "CardDescription": "  /** Renders a <div> styled as muted secondary text below the card title. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "CardFooter": "  /** Renders a <div> with items aligned in a row, typically for actions below CardContent. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "CardHeader": "  /** Wraps a card's title/description block; renders a <div> with vertical flex spacing. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "CardTitle": "  /** Renders a <div> (not a heading element) styled as the card's title text. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "Carousel": "  /** Scroll axis; determines whether left/right or up/down arrows drive navigation. Defaults to \"horizontal\". */\n  orientation?: \"horizontal\" | \"vertical\";\n  /** Options passed to embla-carousel-react's useEmblaCarousel (e.g. loop, align, dragFree). */\n  opts?: Record<string, unknown>;\n  /** Called with the embla carousel API instance once it is initialized. */\n  setApi?: (api: unknown) => void;\n  /** embla-carousel-react plugins array (e.g. autoplay). */\n  plugins?: unknown[];\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div role=\"region\">. */\n  [key: string]: unknown;",
    "CarouselContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div> (the embla scroll track). */\n  [key: string]: unknown;",
    "CarouselItem": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div role=\"group\"> representing a single slide. */\n  [key: string]: unknown;",
    "CarouselNext": "  /** Button visual style, forwarded to the underlying Button. Defaults to \"outline\". */\n  variant?: \"default\" | \"destructive\" | \"outline\" | \"secondary\" | \"ghost\" | \"link\";\n  /** Button size, forwarded to the underlying Button. Defaults to \"icon\". */\n  size?: \"default\" | \"sm\" | \"lg\" | \"icon\";\n  className?: string;\n  /** Remaining props (e.g. onClick/disabled overrides) pass through to the underlying <Button>. */\n  [key: string]: unknown;",
    "CarouselPrevious": "  /** Button visual style, forwarded to the underlying Button. Defaults to \"outline\". */\n  variant?: \"default\" | \"destructive\" | \"outline\" | \"secondary\" | \"ghost\" | \"link\";\n  /** Button size, forwarded to the underlying Button. Defaults to \"icon\". */\n  size?: \"default\" | \"sm\" | \"lg\" | \"icon\";\n  className?: string;\n  /** Remaining props (e.g. onClick/disabled overrides) pass through to the underlying <Button>. */\n  [key: string]: unknown;",
    "ChartContainer": "  /** Optional id suffix; used to scope the generated CSS variables via a `data-chart` attribute. */\n  id?: string;\n  className?: string;\n  /** Chart content, rendered inside a Recharts <ResponsiveContainer>. */\n  children: React.ReactNode;\n  /** Maps each data series key to its label/icon/color (or per-theme colors); used to generate CSS variables and tooltip/legend labels. */\n  config: Record<string, {\n    label?: React.ReactNode;\n    icon?: React.ComponentType;\n    color?: string;\n    theme?: Record<string, string>;\n  }>;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "ChartLegend": "  /** Re-export of Recharts' <Legend>. Legend items, supplied by Recharts. */\n  payload?: Array<Record<string, unknown>>;\n  /** Vertical placement of the legend relative to the chart. */\n  verticalAlign?: \"top\" | \"middle\" | \"bottom\";\n  /** Horizontal alignment of the legend. */\n  align?: \"left\" | \"center\" | \"right\";\n  /** Custom render element/function for the legend — typically <ChartLegendContent />. */\n  content?: React.ReactElement | ((props: Record<string, unknown>) => React.ReactNode);\n  /** Remaining props pass through to Recharts' <Legend>. */\n  [key: string]: unknown;",
    "ChartLegendContent": "  className?: string;\n  /** Hide each item's config icon, falling back to a plain color swatch. */\n  hideIcon?: boolean;\n  /** Legend items, supplied by Recharts. */\n  payload?: Array<Record<string, unknown>>;\n  /** Mirrors Recharts Legend's injected verticalAlign; only \"top\" changes padding direction (default \"bottom\"). */\n  verticalAlign?: \"top\" | \"middle\" | \"bottom\";\n  /** Key used to look up each item's label/icon in the chart config, instead of dataKey. */\n  nameKey?: string;\n  /** This component only reads the named props above; it does not forward a rest spread. */\n  [key: string]: unknown;",
    "ChartStyle": "  /** Must match the `id` passed to the corresponding ChartContainer. */\n  id: string;\n  /** Same shape as ChartContainer's `config`; used to emit per-series CSS color variables. */\n  config: Record<string, {\n    label?: React.ReactNode;\n    icon?: React.ComponentType;\n    color?: string;\n    theme?: Record<string, string>;\n  }>;\n  /** ChartStyle renders only a <style> tag derived from id/config; no other props are read. */\n  [key: string]: unknown;",
    "ChartTooltip": "  /** Re-export of Recharts' <Tooltip>. Whether the tooltip is currently shown. */\n  active?: boolean;\n  /** Data points for the current hover position, supplied by Recharts. */\n  payload?: Array<Record<string, unknown>>;\n  label?: React.ReactNode;\n  /** Cursor highlight rendered behind the tooltip. */\n  cursor?: boolean | React.ReactElement | Record<string, unknown>;\n  /** Custom render element/function for the tooltip body — typically <ChartTooltipContent />. */\n  content?: React.ReactElement | ((props: Record<string, unknown>) => React.ReactNode);\n  /** Remaining props pass through to Recharts' <Tooltip>. */\n  [key: string]: unknown;",
    "ChartTooltipContent": "  /** Whether the tooltip is active, supplied by Recharts when used as Tooltip's `content`. */\n  active?: boolean;\n  /** Data points for the current hover position, supplied by Recharts. */\n  payload?: Array<Record<string, unknown>>;\n  className?: string;\n  /** Visual style of the per-item color indicator. Defaults to \"dot\". */\n  indicator?: \"dot\" | \"line\" | \"dashed\";\n  /** Hide the label row above the item list. */\n  hideLabel?: boolean;\n  /** Hide the per-item color indicator swatch. */\n  hideIndicator?: boolean;\n  label?: React.ReactNode;\n  /** Custom formatter for the label; receives (value, payload). */\n  labelFormatter?: (value: unknown, payload: Array<Record<string, unknown>>) => React.ReactNode;\n  labelClassName?: string;\n  /** Custom formatter for each item; receives (value, name, item, index, payload). */\n  formatter?: (value: unknown, name: unknown, item: Record<string, unknown>, index: number, payload: unknown) => React.ReactNode;\n  /** Fallback indicator color when an item has no fill/color of its own. */\n  color?: string;\n  /** Key used to look up each item's label/icon in the chart config, instead of dataKey/name. */\n  nameKey?: string;\n  /** Key used to look up the label's entry in the chart config, instead of the raw label value. */\n  labelKey?: string;\n  /** This component only reads the named props above; it does not forward a rest spread. */\n  [key: string]: unknown;",
    "Checkbox": "  /**\n   * Checked state. Uncontrolled by default — prefer defaultChecked.\n   * CAVEAT: this component does NOT render a distinct mixed state. Its Indicator always\n   * renders a check glyph and the fill is data-[state=checked]:, so \"indeterminate\" paints a\n   * tick on an unfilled box. Avoid it until checkbox.jsx renders a Minus for that state.\n   */\n  checked?: boolean | \"indeterminate\";\n  defaultChecked?: boolean | \"indeterminate\";\n  onCheckedChange?: (checked: boolean | \"indeterminate\") => void;\n  disabled?: boolean;\n  required?: boolean;\n  name?: string;\n  value?: string;\n  /** Render the child element instead of a <button>, merging props onto it (Radix Slot). */\n  asChild?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix CheckboxPrimitive.Root (<button>). */\n  [key: string]: unknown;",
    "Collapsible": "  /** Controlled open state. */\n  open?: boolean;\n  /** Initial open state when uncontrolled. */\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  disabled?: boolean;\n  /** Render the child element instead of the default wrapper, merging props onto it (Radix Slot). */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix CollapsiblePrimitive.Root. */\n  [key: string]: unknown;",
    "CollapsibleContent": "  /** Keep content mounted in the DOM (e.g. for exit animations) even when closed. */\n  forceMount?: boolean;\n  /** Render the child element instead of a <div>, merging props onto it (Radix Slot). */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix CollapsiblePrimitive.CollapsibleContent (<div>). */\n  [key: string]: unknown;",
    "CollapsibleTrigger": "  /** Render the child element instead of a <button>, merging props onto it (Radix Slot). */\n  asChild?: boolean;\n  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix CollapsiblePrimitive.CollapsibleTrigger (<button>). */\n  [key: string]: unknown;",
    "Command": "  /** Disable cmdk's built-in fuzzy filtering (e.g. to filter results yourself). */\n  shouldFilter?: boolean;\n  /** Custom scoring function used for filtering/sorting items. */\n  filter?: (value: string, search: string, keywords?: string[]) => number;\n  /** Controlled selected item value. */\n  value?: string;\n  onValueChange?: (value: string) => void;\n  /** Accessible label for the command menu. */\n  label?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive (<div>). */\n  [key: string]: unknown;",
    "CommandDialog": "  /** Controlled open state, forwarded to the underlying <Dialog>. */\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Command menu content, rendered inside the dialog's Command wrapper. */\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <Dialog>. */\n  [key: string]: unknown;",
    "CommandEmpty": "  className?: string;\n  /** Content shown when no items match the current search. */\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive.Empty. */\n  [key: string]: unknown;",
    "CommandGroup": "  /** Heading label rendered above the group's items. */\n  heading?: React.ReactNode;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive.Group. */\n  [key: string]: unknown;",
    "CommandInput": "  value?: string;\n  onValueChange?: (search: string) => void;\n  placeholder?: string;\n  className?: string;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive.Input (<input>). */\n  [key: string]: unknown;",
    "CommandItem": "  /** Value used for filtering/selection; defaults to the item's text content if omitted. */\n  value?: string;\n  onSelect?: (value: string) => void;\n  disabled?: boolean;\n  /** Extra search terms matched against, in addition to `value`. */\n  keywords?: string[];\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive.Item. */\n  [key: string]: unknown;",
    "CommandList": "  /** Accessible label for the listbox. */\n  label?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive.List. */\n  [key: string]: unknown;",
    "CommandSeparator": "  /** Render even while the user has typed a search query (normally hidden while filtering). */\n  alwaysRender?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying cmdk CommandPrimitive.Separator. */\n  [key: string]: unknown;",
    "CommandShortcut": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <span>. */\n  [key: string]: unknown;",
    "ContextMenu": "  /** Fires when the menu opens or closes (e.g. via right-click or Escape). */\n  onOpenChange?: (open: boolean) => void;\n  /** Text direction for keyboard navigation. */\n  dir?: \"ltr\" | \"rtl\";\n  /** When true, interaction with outside elements is disabled and only menu content is visible to screen readers. */\n  modal?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Root. */\n  [key: string]: unknown;",
    "ContextMenuCheckboxItem": "  /** Controls the checked state; use \"indeterminate\" for a mixed state. */\n  checked?: boolean | \"indeterminate\";\n  /** Called when the checked state changes. */\n  onCheckedChange?: (checked: boolean) => void;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.CheckboxItem. */\n  [key: string]: unknown;",
    "ContextMenuContent": "  /** Loop keyboard focus back to the first/last item at the boundaries. */\n  loop?: boolean;\n  onCloseAutoFocus?: (event: Event) => void;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  alignOffset?: number;\n  avoidCollisions?: boolean;\n  collisionBoundary?: Element | null | Array<Element | null>;\n  collisionPadding?: number | Partial<Record<\"top\" | \"right\" | \"bottom\" | \"left\", number>>;\n  sticky?: \"partial\" | \"always\";\n  hideWhenDetached?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Content (rendered inside a Portal). */\n  [key: string]: unknown;",
    "ContextMenuGroup": "  /** Render as the passed child element instead of the default wrapper. */\n  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Group. */\n  [key: string]: unknown;",
    "ContextMenuItem": "  /** Indents the item to align with items that have a leading icon/checkbox. */\n  inset?: boolean;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Item. */\n  [key: string]: unknown;",
    "ContextMenuLabel": "  /** Indents the label to align with inset items. */\n  inset?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Label. */\n  [key: string]: unknown;",
    "ContextMenuPortal": "  /** Force mounting when used with animation libraries. */\n  forceMount?: true;\n  /** Custom container to portal into; defaults to document.body. */\n  container?: HTMLElement | null;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Portal. */\n  [key: string]: unknown;",
    "ContextMenuRadioGroup": "  /** Currently selected radio value. */\n  value?: string;\n  /** Called when the selected value changes. */\n  onValueChange?: (value: string) => void;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.RadioGroup. */\n  [key: string]: unknown;",
    "ContextMenuRadioItem": "  /** Unique value identifying this radio option within its group. */\n  value: string;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.RadioItem. */\n  [key: string]: unknown;",
    "ContextMenuSeparator": "  className?: string;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Separator. */\n  [key: string]: unknown;",
    "ContextMenuShortcut": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <span>. */\n  [key: string]: unknown;",
    "ContextMenuSub": "  /** Controlled open state of the submenu. */\n  open?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  defaultOpen?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Sub. */\n  [key: string]: unknown;",
    "ContextMenuSubContent": "  loop?: boolean;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  alignOffset?: number;\n  avoidCollisions?: boolean;\n  collisionBoundary?: Element | null | Array<Element | null>;\n  collisionPadding?: number | Partial<Record<\"top\" | \"right\" | \"bottom\" | \"left\", number>>;\n  sticky?: \"partial\" | \"always\";\n  hideWhenDetached?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.SubContent. */\n  [key: string]: unknown;",
    "ContextMenuSubTrigger": "  /** Indents the trigger to align with items that have a leading icon/checkbox. */\n  inset?: boolean;\n  disabled?: boolean;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.SubTrigger. */\n  [key: string]: unknown;",
    "ContextMenuTrigger": "  disabled?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ContextMenu.Trigger. */\n  [key: string]: unknown;",
    "Dialog": "  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** When true, interaction with outside elements is disabled. */\n  modal?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Root. */\n  [key: string]: unknown;",
    "DialogClose": "  /** Render the child element instead of the default button, merging props onto it. */\n  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Close. */\n  [key: string]: unknown;",
    "DialogContent": "  onOpenAutoFocus?: (event: Event) => void;\n  onCloseAutoFocus?: (event: Event) => void;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  onInteractOutside?: (event: Event) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Content. Renders inside a DialogPortal with a DialogOverlay and a default close (X) button. */\n  [key: string]: unknown;",
    "DialogDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Description. */\n  [key: string]: unknown;",
    "DialogFooter": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "DialogHeader": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "DialogOverlay": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Overlay. */\n  [key: string]: unknown;",
    "DialogPortal": "  /** Force mounting when used with animation libraries. */\n  forceMount?: true;\n  /** Custom container to portal into; defaults to document.body. */\n  container?: HTMLElement | null;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Portal. */\n  [key: string]: unknown;",
    "DialogTitle": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Title. */\n  [key: string]: unknown;",
    "DialogTrigger": "  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog.Trigger. */\n  [key: string]: unknown;",
    "Drawer": "  /** Scales and moves the page content behind the drawer to create a native-feeling background effect. Defaults to true. */\n  shouldScaleBackground?: boolean;\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Edge the drawer slides in from. */\n  direction?: \"top\" | \"bottom\" | \"left\" | \"right\";\n  /** Whether the drawer can be dismissed by dragging or clicking outside. */\n  dismissible?: boolean;\n  modal?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Root. */\n  [key: string]: unknown;",
    "DrawerClose": "  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Close. */\n  [key: string]: unknown;",
    "DrawerContent": "  onOpenAutoFocus?: (event: Event) => void;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  onInteractOutside?: (event: Event) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Content. Renders inside a DrawerPortal with a DrawerOverlay and a drag handle bar. */\n  [key: string]: unknown;",
    "DrawerDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Description. */\n  [key: string]: unknown;",
    "DrawerFooter": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "DrawerHeader": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "DrawerOverlay": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Overlay. */\n  [key: string]: unknown;",
    "DrawerPortal": "  /** Custom container to portal into; defaults to document.body. */\n  container?: HTMLElement | null;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Portal. */\n  [key: string]: unknown;",
    "DrawerTitle": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Title. */\n  [key: string]: unknown;",
    "DrawerTrigger": "  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying vaul Drawer.Trigger. */\n  [key: string]: unknown;",
    "DropdownMenu": "  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  dir?: \"ltr\" | \"rtl\";\n  modal?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Root. */\n  [key: string]: unknown;",
    "DropdownMenuCheckboxItem": "  checked?: boolean | \"indeterminate\";\n  onCheckedChange?: (checked: boolean) => void;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.CheckboxItem. */\n  [key: string]: unknown;",
    "DropdownMenuContent": "  /** Distance in pixels from the trigger. Defaults to 4. */\n  sideOffset?: number;\n  side?: \"top\" | \"right\" | \"bottom\" | \"left\";\n  align?: \"start\" | \"center\" | \"end\";\n  alignOffset?: number;\n  avoidCollisions?: boolean;\n  collisionBoundary?: Element | null | Array<Element | null>;\n  collisionPadding?: number | Partial<Record<\"top\" | \"right\" | \"bottom\" | \"left\", number>>;\n  sticky?: \"partial\" | \"always\";\n  hideWhenDetached?: boolean;\n  loop?: boolean;\n  onCloseAutoFocus?: (event: Event) => void;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Content (rendered inside a Portal). */\n  [key: string]: unknown;",
    "DropdownMenuGroup": "  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Group. */\n  [key: string]: unknown;",
    "DropdownMenuItem": "  /** Indents the item to align with items that have a leading icon/checkbox. */\n  inset?: boolean;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Item. */\n  [key: string]: unknown;",
    "DropdownMenuLabel": "  /** Indents the label to align with inset items. */\n  inset?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Label. */\n  [key: string]: unknown;",
    "DropdownMenuPortal": "  /** Force mounting when used with animation libraries. */\n  forceMount?: true;\n  /** Custom container to portal into; defaults to document.body. */\n  container?: HTMLElement | null;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Portal. */\n  [key: string]: unknown;",
    "DropdownMenuRadioGroup": "  value?: string;\n  onValueChange?: (value: string) => void;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.RadioGroup. */\n  [key: string]: unknown;",
    "DropdownMenuRadioItem": "  value: string;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.RadioItem. */\n  [key: string]: unknown;",
    "DropdownMenuSeparator": "  className?: string;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Separator. */\n  [key: string]: unknown;",
    "DropdownMenuShortcut": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <span>. */\n  [key: string]: unknown;",
    "DropdownMenuSub": "  open?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  defaultOpen?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Sub. */\n  [key: string]: unknown;",
    "DropdownMenuSubContent": "  loop?: boolean;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  alignOffset?: number;\n  avoidCollisions?: boolean;\n  collisionBoundary?: Element | null | Array<Element | null>;\n  collisionPadding?: number | Partial<Record<\"top\" | \"right\" | \"bottom\" | \"left\", number>>;\n  sticky?: \"partial\" | \"always\";\n  hideWhenDetached?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.SubContent. */\n  [key: string]: unknown;",
    "DropdownMenuSubTrigger": "  /** Indents the trigger to align with items that have a leading icon/checkbox. */\n  inset?: boolean;\n  disabled?: boolean;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.SubTrigger. */\n  [key: string]: unknown;",
    "DropdownMenuTrigger": "  asChild?: boolean;\n  disabled?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix DropdownMenu.Trigger. */\n  [key: string]: unknown;",
    "Form": "  /** Alias for react-hook-form's FormProvider; spread the object returned by useForm() as props. */\n  control?: unknown;\n  handleSubmit?: (...args: unknown[]) => unknown;\n  register?: (...args: unknown[]) => unknown;\n  formState?: Record<string, unknown>;\n  watch?: (...args: unknown[]) => unknown;\n  getValues?: (...args: unknown[]) => unknown;\n  setValue?: (...args: unknown[]) => unknown;\n  reset?: (...args: unknown[]) => unknown;\n  trigger?: (...args: unknown[]) => unknown;\n  children?: React.ReactNode;\n  /** Remaining props pass through to react-hook-form's FormProvider (the full UseFormReturn object). */\n  [key: string]: unknown;",
    "FormControl": "  /** A single child element that receives the form field's id, aria-describedby, and aria-invalid via Radix Slot. */\n  children?: React.ReactNode;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix Slot, merged onto the single child element. */\n  [key: string]: unknown;",
    "FormDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <p>. */\n  [key: string]: unknown;",
    "FormField": "  /** Field name; must match a key in the form's values. */\n  name: string;\n  /** The react-hook-form control object from useForm(); optional when used within a <Form> (FormProvider) context. */\n  control?: unknown;\n  /** Render prop receiving { field, fieldState, formState } and returning the field's markup. */\n  render: (props: { field: unknown; fieldState: unknown; formState: unknown }) => React.ReactElement;\n  defaultValue?: unknown;\n  /** Validation rules (react-hook-form RegisterOptions). */\n  rules?: Record<string, unknown>;\n  shouldUnregister?: boolean;\n  /** Remaining props pass through to react-hook-form's Controller. */\n  [key: string]: unknown;",
    "FormItem": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. Generates a unique id shared by this item's label, description, and message. */\n  [key: string]: unknown;",
    "FormLabel": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Label component (Radix Label.Root). Automatically applies destructive styling and htmlFor when the field has a validation error. */\n  [key: string]: unknown;",
    "FormMessage": "  className?: string;\n  /** Custom message content; ignored (replaced by the field's validation error message) when the field is in an error state. Renders nothing if there is no error and no children. */\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <p>. */\n  [key: string]: unknown;",
    "HoverCard": "  /** Controlled open state of the hover card. */\n  open?: boolean;\n  /** Uncontrolled open state on first render. */\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Delay in ms before the card opens after the trigger is hovered. Defaults to 700. */\n  openDelay?: number;\n  /** Delay in ms before the card closes after the pointer leaves. Defaults to 300. */\n  closeDelay?: number;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix HoverCard.Root. */\n  [key: string]: unknown;",
    "HoverCardContent": "  /** Alignment relative to the trigger. Defaults to \"center\". */\n  align?: \"start\" | \"center\" | \"end\";\n  /** Offset in pixels along the alignment axis. */\n  alignOffset?: number;\n  /** Preferred side to render on relative to the trigger. */\n  side?: \"top\" | \"right\" | \"bottom\" | \"left\";\n  /** Offset in pixels from the trigger. Defaults to 4. */\n  sideOffset?: number;\n  /** Prevent collisions with viewport edges by flipping/shifting. */\n  avoidCollisions?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix HoverCard.Content. */\n  [key: string]: unknown;",
    "HoverCardTrigger": "  /** Render the child element instead of the default trigger element, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix HoverCard.Trigger (renders an <a> by default). */\n  [key: string]: unknown;",
    "Input": "  /** Native input type, e.g. \"text\", \"email\", \"password\", \"number\". */\n  type?: React.HTMLInputTypeAttribute;\n  placeholder?: string;\n  value?: string | number | readonly string[];\n  defaultValue?: string | number | readonly string[];\n  onChange?: (event: React.ChangeEvent<HTMLInputElement>) => void;\n  disabled?: boolean;\n  required?: boolean;\n  name?: string;\n  readOnly?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying <input>. */\n  [key: string]: unknown;",
    "InputOTP": "  /** Total number of OTP characters/slots. Required by the underlying OTPInput. */\n  maxLength: number;\n  /** Current value of the input. */\n  value?: string;\n  onChange?: (value: string) => void;\n  /** Called when the value reaches maxLength. */\n  onComplete?: (value: string) => void;\n  disabled?: boolean;\n  /** Restrict allowed characters, e.g. a regex-like pattern string. */\n  pattern?: string;\n  inputMode?: \"numeric\" | \"text\" | \"tel\" | \"search\";\n  /** className applied to the outer container div (has-[:disabled]:opacity-50 wrapper). */\n  containerClassName?: string;\n  /** className applied to the underlying input element. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying OTPInput from \"input-otp\". */\n  [key: string]: unknown;",
    "InputOTPGroup": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "InputOTPSeparator": "  className?: string;\n  /** Remaining props pass through to the underlying <div role=\"separator\">; its content (a Minus icon) is fixed and not configurable via children. */\n  [key: string]: unknown;",
    "InputOTPSlot": "  /** Zero-based index of this slot into the OTP value; used to read this slot's char/caret/active state from OTPInputContext. */\n  index: number;\n  className?: string;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "Label": "  /** id of the form control this label is bound to. */\n  htmlFor?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Label.Root (<label>). */\n  [key: string]: unknown;",
    "Menubar": "  /** Controlled value of the currently open menu. */\n  value?: string;\n  defaultValue?: string;\n  onValueChange?: (value: string) => void;\n  /** Whether keyboard navigation loops from the last item back to the first. */\n  loop?: boolean;\n  dir?: \"ltr\" | \"rtl\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Root. */\n  [key: string]: unknown;",
    "MenubarCheckboxItem": "  /** Whether the item is checked; also supports the indeterminate state. */\n  checked?: boolean | \"indeterminate\";\n  onCheckedChange?: (checked: boolean) => void;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.CheckboxItem. */\n  [key: string]: unknown;",
    "MenubarContent": "  /** Alignment relative to the menu's trigger. Defaults to \"start\". */\n  align?: \"start\" | \"center\" | \"end\";\n  /** Offset in pixels along the alignment axis. Defaults to -4. */\n  alignOffset?: number;\n  /** Preferred side to render on relative to the trigger. */\n  side?: \"top\" | \"right\" | \"bottom\" | \"left\";\n  /** Offset in pixels from the trigger. Defaults to 8. */\n  sideOffset?: number;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Content (rendered inside a Portal). */\n  [key: string]: unknown;",
    "MenubarGroup": "  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Group. */\n  [key: string]: unknown;",
    "MenubarItem": "  /** Indents the item to align with checkbox/radio items in the same menu. */\n  inset?: boolean;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Item. */\n  [key: string]: unknown;",
    "MenubarLabel": "  /** Indents the label to align with checkbox/radio items. */\n  inset?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Label. */\n  [key: string]: unknown;",
    "MenubarMenu": "  /** Value identifying this menu within the menubar. */\n  value?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Menu. */\n  [key: string]: unknown;",
    "MenubarPortal": "  /** Force mounting when more control is needed over animation with React presence. */\n  forceMount?: true;\n  /** Custom container element to portal the content into. */\n  container?: Element | DocumentFragment | null;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Portal. */\n  [key: string]: unknown;",
    "MenubarRadioGroup": "  value?: string;\n  onValueChange?: (value: string) => void;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.RadioGroup. */\n  [key: string]: unknown;",
    "MenubarRadioItem": "  /** Value of this radio item within its enclosing MenubarRadioGroup. */\n  value: string;\n  disabled?: boolean;\n  onSelect?: (event: Event) => void;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.RadioItem. */\n  [key: string]: unknown;",
    "MenubarSeparator": "  className?: string;\n  /** Remaining props pass through to the underlying Radix Menubar.Separator. */\n  [key: string]: unknown;",
    "MenubarShortcut": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <span>. */\n  [key: string]: unknown;",
    "MenubarSub": "  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Sub. */\n  [key: string]: unknown;",
    "MenubarSubContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.SubContent. */\n  [key: string]: unknown;",
    "MenubarSubTrigger": "  /** Indents the trigger to align with checkbox/radio items in the parent menu. */\n  inset?: boolean;\n  disabled?: boolean;\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.SubTrigger; a chevron-right icon is appended automatically. */\n  [key: string]: unknown;",
    "MenubarTrigger": "  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Menubar.Trigger. */\n  [key: string]: unknown;",
    "NavigationMenu": "  /** Controlled value of the currently active menu item. */\n  value?: string;\n  defaultValue?: string;\n  onValueChange?: (value: string) => void;\n  /** Delay in ms before opening a menu item on hover. */\n  delayDuration?: number;\n  /** Delay in ms for switching between items without closing in between. */\n  skipDelayDuration?: number;\n  orientation?: \"horizontal\" | \"vertical\";\n  dir?: \"ltr\" | \"rtl\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Root; a NavigationMenuViewport is always rendered after children. */\n  [key: string]: unknown;",
    "NavigationMenuContent": "  /** Force mounting when more control is needed over animation with React presence. */\n  forceMount?: true;\n  onEscapeKeyDown?: (event: KeyboardEvent) => void;\n  onPointerDownOutside?: (event: Event) => void;\n  onFocusOutside?: (event: Event) => void;\n  onInteractOutside?: (event: Event) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Content. */\n  [key: string]: unknown;",
    "NavigationMenuIndicator": "  /** Force mounting when more control is needed over animation with React presence. */\n  forceMount?: true;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Indicator; its arrow content is fixed and not configurable via children. */\n  [key: string]: unknown;",
    "NavigationMenuItem": "  /** Value identifying this item; used to control which item is active. */\n  value?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Item. */\n  [key: string]: unknown;",
    "NavigationMenuLink": "  /** Marks this link as representing the current page. */\n  active?: boolean;\n  onSelect?: (event: Event) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Link (renders an <a> by default). */\n  [key: string]: unknown;",
    "NavigationMenuList": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.List (<ul>). */\n  [key: string]: unknown;",
    "NavigationMenuTrigger": "  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Trigger; a chevron indicator icon is appended automatically. */\n  [key: string]: unknown;",
    "NavigationMenuViewport": "  /** Force mounting when more control is needed over animation with React presence. */\n  forceMount?: true;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix NavigationMenu.Viewport (wrapped in a positioning <div>). */\n  [key: string]: unknown;",
    "Pagination": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <nav role=\"navigation\" aria-label=\"pagination\"> element. */\n  [key: string]: unknown;",
    "PaginationContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <ul> element. */\n  [key: string]: unknown;",
    "PaginationEllipsis": "  className?: string;\n  /** Remaining props pass through to the underlying <span aria-hidden> element. Fixed icon/label content -- children are not rendered. */\n  [key: string]: unknown;",
    "PaginationItem": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <li> element. */\n  [key: string]: unknown;",
    "PaginationLink": "  /** Marks this link as the current page: sets aria-current=\"page\" and switches the visual variant to \"outline\" (vs. \"ghost\" when false). */\n  isActive?: boolean;\n  /** Visual size, forwarded to Button's buttonVariants size scale. Defaults to \"icon\". */\n  size?: \"default\" | \"sm\" | \"lg\" | \"icon\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <a> element (e.g. href, onClick). */\n  [key: string]: unknown;",
    "PaginationNext": "  className?: string;\n  /** Renders a PaginationLink with a fixed \"Next\" label/icon and size=\"default\"; remaining props (e.g. href, onClick) pass through to the underlying <a> element. */\n  [key: string]: unknown;",
    "PaginationPrevious": "  className?: string;\n  /** Renders a PaginationLink with a fixed \"Previous\" label/icon and size=\"default\"; remaining props (e.g. href, onClick) pass through to the underlying <a> element. */\n  [key: string]: unknown;",
    "Popover": "  /** Controlled open state. */\n  open?: boolean;\n  /** Uncontrolled initial open state. */\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Renders content as a modal, trapping focus and blocking outside interaction. */\n  modal?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Popover.Root. */\n  [key: string]: unknown;",
    "PopoverAnchor": "  /** Render the child element as the anchor instead of a wrapping element. */\n  asChild?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Popover.Anchor. */\n  [key: string]: unknown;",
    "PopoverContent": "  /** Alignment relative to the trigger/anchor. Defaults to \"center\". */\n  align?: \"start\" | \"center\" | \"end\";\n  /** Distance in pixels from the trigger/anchor. Defaults to 4. */\n  sideOffset?: number;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Popover.Content (rendered inside a Portal), e.g. side, alignOffset, onOpenAutoFocus, onPointerDownOutside. */\n  [key: string]: unknown;",
    "PopoverTrigger": "  /** Render the child element instead of a default trigger button, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Popover.Trigger (a <button> by default). */\n  [key: string]: unknown;",
    "Progress": "  /** Current progress value (0-100), used to compute the indicator's translateX offset. Defaults to 0 when omitted. */\n  value?: number;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix Progress.Root, e.g. max, getValueLabel. */\n  [key: string]: unknown;",
    "RadioGroup": "  /** Controlled selected value. */\n  value?: string;\n  /** Uncontrolled initial selected value. */\n  defaultValue?: string;\n  onValueChange?: (value: string) => void;\n  disabled?: boolean;\n  name?: string;\n  required?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix RadioGroup.Root, e.g. orientation, dir, loop. */\n  [key: string]: unknown;",
    "RadioGroupItem": "  /** The value submitted when this item is selected. */\n  value: string;\n  disabled?: boolean;\n  required?: boolean;\n  id?: string;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix RadioGroup.Item (renders a filled-circle indicator when checked). */\n  [key: string]: unknown;",
    "ResizableHandle": "  /** Shows a small grip handle indicator in the middle of the resize bar. */\n  withHandle?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying react-resizable-panels PanelResizeHandle. */\n  [key: string]: unknown;",
    "ResizablePanel": "  /** Initial size as a percentage of the group (0-100). */\n  defaultSize?: number;\n  /** Minimum size as a percentage of the group. */\n  minSize?: number;\n  /** Maximum size as a percentage of the group. */\n  maxSize?: number;\n  /** Allows this panel to collapse to size 0. */\n  collapsible?: boolean;\n  /** Order within the group; required when panels are conditionally rendered. */\n  order?: number;\n  id?: string;\n  onResize?: (size: number) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Bare alias for react-resizable-panels' Panel -- remaining props pass through directly to it. */\n  [key: string]: unknown;",
    "ResizablePanelGroup": "  /** Layout direction of the panel group. */\n  direction: \"horizontal\" | \"vertical\";\n  /** Persists panel sizes (e.g. to localStorage) under this id between sessions. */\n  autoSaveId?: string;\n  onLayout?: (sizes: number[]) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying react-resizable-panels PanelGroup. */\n  [key: string]: unknown;",
    "ScrollArea": "  className?: string;\n  children?: React.ReactNode;\n  /** Controls when scrollbars are visible. */\n  type?: \"auto\" | \"always\" | \"scroll\" | \"hover\";\n  /** Delay in ms before auto-hiding scrollbars (used with type \"scroll\" or \"hover\"). */\n  scrollHideDelay?: number;\n  /** Remaining props pass through to the underlying Radix ScrollArea.Root. A ScrollBar and Corner are rendered automatically. */\n  [key: string]: unknown;",
    "ScrollBar": "  /** Scrollbar orientation. Defaults to \"vertical\". */\n  orientation?: \"vertical\" | \"horizontal\";\n  className?: string;\n  /** Remaining props pass through to the underlying Radix ScrollArea.Scrollbar. */\n  [key: string]: unknown;",
    "Select": "  /** Controlled selected value. */\n  value?: string;\n  /** Uncontrolled initial selected value. */\n  defaultValue?: string;\n  onValueChange?: (value: string) => void;\n  /** Controlled open state. */\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  name?: string;\n  disabled?: boolean;\n  required?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Root. */\n  [key: string]: unknown;",
    "SelectContent": "  /** Positioning strategy. \"popper\" (the default used here) anchors like a popover next to the trigger; \"item-aligned\" overlaps the selected item on the trigger, mimicking a native select. */\n  position?: \"popper\" | \"item-aligned\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Content (rendered inside a Portal), e.g. side, sideOffset, align, onCloseAutoFocus. */\n  [key: string]: unknown;",
    "SelectGroup": "  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Group. */\n  [key: string]: unknown;",
    "SelectItem": "  /** The value submitted when this item is selected. */\n  value: string;\n  disabled?: boolean;\n  /** Used for type-ahead matching when the rendered children aren't plain text. */\n  textValue?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Item. A check indicator is rendered automatically when selected. */\n  [key: string]: unknown;",
    "SelectLabel": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Label. */\n  [key: string]: unknown;",
    "SelectScrollDownButton": "  className?: string;\n  /** Remaining props pass through to the underlying Radix Select.ScrollDownButton. */\n  [key: string]: unknown;",
    "SelectScrollUpButton": "  className?: string;\n  /** Remaining props pass through to the underlying Radix Select.ScrollUpButton. */\n  [key: string]: unknown;",
    "SelectSeparator": "  className?: string;\n  /** Remaining props pass through to the underlying Radix Select.Separator. */\n  [key: string]: unknown;",
    "SelectTrigger": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Trigger (a <button>); a chevron-down indicator icon is appended automatically. */\n  [key: string]: unknown;",
    "SelectValue": "  /** Placeholder content shown when no value is selected. */\n  placeholder?: React.ReactNode;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Select.Value. */\n  [key: string]: unknown;",
    "Separator": "  /** Orientation of the separator line. Defaults to \"horizontal\". */\n  orientation?: \"horizontal\" | \"vertical\";\n  /** When true (default), marks the separator as purely visual so assistive tech ignores it; set false if it's a semantically meaningful divider. */\n  decorative?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix Separator.Root. */\n  [key: string]: unknown;",
    "Sheet": "  /** Controlled open state. Omit for uncontrolled usage with `defaultOpen`. */\n  open?: boolean;\n  /** Initial open state when uncontrolled. */\n  defaultOpen?: boolean;\n  /** Called when the open state changes, e.g. Escape key or overlay click. */\n  onOpenChange?: (open: boolean) => void;\n  /** When false, interaction outside the sheet is not blocked and no overlay is rendered. */\n  modal?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog Root. */\n  [key: string]: unknown;",
    "SheetClose": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <button>. */\n  [key: string]: unknown;",
    "SheetContent": "  /** Which edge of the viewport the sheet slides in from. Defaults to \"right\". */\n  side?: \"top\" | \"right\" | \"bottom\" | \"left\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog Content. */\n  [key: string]: unknown;",
    "SheetDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog Description (renders a <p>). */\n  [key: string]: unknown;",
    "SheetFooter": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SheetHeader": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SheetOverlay": "  className?: string;\n  /** Remaining props pass through to the underlying Radix Dialog Overlay. */\n  [key: string]: unknown;",
    "SheetPortal": "  children?: React.ReactNode;\n  /** Target DOM node to render the portal into; defaults to document.body. */\n  container?: HTMLElement | null;\n  /** Force mounting, useful when animating with CSS. */\n  forceMount?: true;\n  /** Remaining props pass through to the underlying Radix Dialog Portal. */\n  [key: string]: unknown;",
    "SheetTitle": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Dialog Title (renders an <h2>). */\n  [key: string]: unknown;",
    "SheetTrigger": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <button>. */\n  [key: string]: unknown;",
    "Sidebar": "  /** Which side of the viewport the sidebar is docked to. Defaults to \"left\". */\n  side?: \"left\" | \"right\";\n  /** Visual style of the sidebar container. Defaults to \"sidebar\". */\n  variant?: \"sidebar\" | \"floating\" | \"inset\";\n  /** How the sidebar collapses on desktop: fully off-canvas, shrunk to an icon rail, or never. Defaults to \"offcanvas\". */\n  collapsible?: \"offcanvas\" | \"icon\" | \"none\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div> (or Sheet content on mobile). Requires a SidebarProvider ancestor. */\n  [key: string]: unknown;",
    "SidebarContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarFooter": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarGroup": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarGroupAction": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <button>. */\n  [key: string]: unknown;",
    "SidebarGroupContent": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarGroupLabel": "  /** Render the child element instead of a <div>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarHeader": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarInput": "  className?: string;\n  /** Remaining props pass through to the underlying Input component (renders an <input>). */\n  [key: string]: unknown;",
    "SidebarInset": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <main>. */\n  [key: string]: unknown;",
    "SidebarMenu": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <ul>. */\n  [key: string]: unknown;",
    "SidebarMenuAction": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  /** Only reveal the action on hover/focus of the parent menu item instead of always showing it. */\n  showOnHover?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <button>. */\n  [key: string]: unknown;",
    "SidebarMenuBadge": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarMenuButton": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  /** Marks this as the current/active item (adds active styling and data-active). */\n  isActive?: boolean;\n  /** Visual style. Defaults to \"default\". */\n  variant?: \"default\" | \"outline\";\n  /** Size/density. Defaults to \"default\". */\n  size?: \"default\" | \"sm\" | \"lg\";\n  /**\n   * Tooltip shown when the sidebar is collapsed to icon mode. A string is used as the\n   * tooltip's children; an object is spread onto the underlying TooltipContent (e.g. { children, side, align }).\n   */\n  tooltip?: string | { children?: React.ReactNode; [key: string]: unknown };\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <button>. Requires a SidebarProvider ancestor. */\n  [key: string]: unknown;",
    "SidebarMenuItem": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <li>. */\n  [key: string]: unknown;",
    "SidebarMenuSkeleton": "  /** Render a leading icon-sized placeholder alongside the text placeholder. */\n  showIcon?: boolean;\n  className?: string;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "SidebarMenuSub": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <ul>. */\n  [key: string]: unknown;",
    "SidebarMenuSubButton": "  /** Render the child element instead of an <a>, merging props onto it. */\n  asChild?: boolean;\n  /** Text size of the link. Defaults to \"md\". */\n  size?: \"sm\" | \"md\";\n  /** Marks this as the current/active item. */\n  isActive?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <a>. */\n  [key: string]: unknown;",
    "SidebarMenuSubItem": "  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <li>. */\n  [key: string]: unknown;",
    "SidebarProvider": "  /**\n   * Required context provider for every Sidebar* component in this file — Sidebar,\n   * SidebarTrigger, SidebarMenuButton, etc. all call useSidebar() internally and throw\n   * if rendered without an ancestor SidebarProvider.\n   * Initial open state when uncontrolled.\n   */\n  defaultOpen?: boolean;\n  /** Controlled open state; pair with onOpenChange to control from outside. */\n  open?: boolean;\n  /** Called whenever the sidebar's open state changes, including via the Cmd/Ctrl+B shortcut. */\n  onOpenChange?: (open: boolean) => void;\n  className?: string;\n  style?: React.CSSProperties;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <div> wrapper. */\n  [key: string]: unknown;",
    "SidebarRail": "  className?: string;\n  /** Remaining props pass through to the underlying <button> (thin edge handle that toggles the sidebar). Requires a SidebarProvider ancestor. */\n  [key: string]: unknown;",
    "SidebarSeparator": "  className?: string;\n  /** Remaining props pass through to the underlying Separator component. */\n  [key: string]: unknown;",
    "SidebarTrigger": "  /** Render the child element instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  /** Called before the sidebar's open state is toggled. */\n  onClick?: (event: React.MouseEvent<HTMLButtonElement>) => void;\n  className?: string;\n  /** Remaining props pass through to the underlying Button component. Requires a SidebarProvider ancestor. */\n  [key: string]: unknown;",
    "Skeleton": "  className?: string;\n  /** Remaining props pass through to the underlying <div>. */\n  [key: string]: unknown;",
    "Slider": "  /**\n   * Current value(s) for a controlled slider. CAVEAT: this component renders exactly one\n   * hardcoded Thumb, so only a SINGLE-value array works. A two-value range renders one thumb\n   * and mis-tracks.\n   */\n  value?: number[];\n  /** Initial value(s) for an uncontrolled slider. */\n  defaultValue?: number[];\n  /** Called continuously as the value changes while dragging. */\n  onValueChange?: (value: number[]) => void;\n  /** Called once the user finishes dragging/interacting. */\n  onValueCommit?: (value: number[]) => void;\n  min?: number;\n  max?: number;\n  step?: number;\n  /**\n   * Blocks interaction. CAVEAT: it has NO visual effect in this component — the\n   * disabled: styles sit on the Thumb, which Radix renders as a <span>, and CSS :disabled\n   * only matches form controls. A disabled Slider looks identical to an enabled one.\n   */\n  disabled?: boolean;\n  /** Layout axis. Defaults to \"horizontal\". */\n  orientation?: \"horizontal\" | \"vertical\";\n  className?: string;\n  /** Remaining props pass through to the underlying Radix Slider Root. */\n  [key: string]: unknown;",
    "Switch": "  /** Controlled checked state. */\n  checked?: boolean;\n  /** Initial checked state when uncontrolled. */\n  defaultChecked?: boolean;\n  /** Called when the checked state changes. */\n  onCheckedChange?: (checked: boolean) => void;\n  disabled?: boolean;\n  required?: boolean;\n  name?: string;\n  value?: string;\n  className?: string;\n  /** Remaining props pass through to the underlying Radix Switch Root (renders a <button role=\"switch\">). */\n  [key: string]: unknown;",
    "Table": "  /** Renders a scrollable wrapper <div className=\"relative w-full overflow-auto\"> around the table; className is applied to the inner <table> element, not the wrapper div. */\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <table>. */\n  [key: string]: unknown;",
    "TableBody": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <tbody>. */\n  [key: string]: unknown;",
    "TableCaption": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <caption>. */\n  [key: string]: unknown;",
    "TableCell": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <td>. */\n  [key: string]: unknown;",
    "TableFooter": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <tfoot>. */\n  [key: string]: unknown;",
    "TableHead": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <th>. */\n  [key: string]: unknown;",
    "TableHeader": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <thead>. */\n  [key: string]: unknown;",
    "TableRow": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying <tr>. */\n  [key: string]: unknown;",
    "Tabs": "  /** The value of the tab that should be active when initially rendered (uncontrolled). */\n  defaultValue?: string;\n  /** The controlled value of the active tab. Must be paired with onValueChange. */\n  value?: string;\n  onValueChange?: (value: string) => void;\n  /** Layout direction used for arrow-key navigation. */\n  orientation?: \"horizontal\" | \"vertical\";\n  dir?: \"ltr\" | \"rtl\";\n  /** Whether a tab is activated automatically on focus, or only via explicit click/Enter. */\n  activationMode?: \"automatic\" | \"manual\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tabs.Root (renders a <div>). */\n  [key: string]: unknown;",
    "TabsContent": "  /** Matches the value of its associated TabsTrigger. Required. */\n  value: string;\n  /** Force the content to be mounted even when its tab is inactive (e.g. for animation). */\n  forceMount?: true;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tabs.Content (renders a <div role=\"tabpanel\">). */\n  [key: string]: unknown;",
    "TabsList": "  /** Whether keyboard navigation should loop from last tab back to first. */\n  loop?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tabs.List (renders a <div role=\"tablist\">). */\n  [key: string]: unknown;",
    "TabsTrigger": "  /** Matches the value of its associated TabsContent. Required. */\n  value: string;\n  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tabs.Trigger (renders a <button role=\"tab\">). */\n  [key: string]: unknown;",
    "Textarea": "  /** Additional class names merged with the default textarea styles (border, focus ring, disabled state, etc.). */\n  className?: string;\n  /** Remaining props pass through to the underlying <textarea> element (value, defaultValue, placeholder, rows, disabled, onChange, ...). Note: use value/defaultValue rather than children on a textarea. */\n  [key: string]: unknown;",
    "Toast": "  /** Visual style; drives the destructive (red) vs default toast look. */\n  variant?: \"default\" | \"destructive\";\n  /** Whether the toast is time-sensitive (announced immediately) or can be announced when the user is idle. */\n  type?: \"foreground\" | \"background\";\n  /** Time in ms before this toast auto-dismisses; overrides ToastProvider's duration. */\n  duration?: number;\n  /** Controlled open state. */\n  open?: boolean;\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toast.Root (renders a <li>); includes swipe/escape callbacks like onSwipeEnd, onEscapeKeyDown. */\n  [key: string]: unknown;",
    "ToastAction": "  /** Required by Radix: a plain-text description of the action, announced to screen-reader users in place of the button's visual content. */\n  altText: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toast.Action (renders a <button>). */\n  [key: string]: unknown;",
    "ToastClose": "  className?: string;\n  /** ToastClose always renders its own close (X) icon internally; it does not accept custom children. */\n  [key: string]: unknown;",
    "ToastDescription": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toast.Description (renders a <div>). */\n  [key: string]: unknown;",
    "ToastProvider": "  /** Screen-reader label for the region announcing toasts (defaults to Radix's built-in label). */\n  label?: string;\n  /** Default time in ms before a toast is auto-dismissed. */\n  duration?: number;\n  /** Direction the toast may be swiped to dismiss it. */\n  swipeDirection?: \"right\" | \"left\" | \"up\" | \"down\";\n  /** Distance in pixels a swipe must travel before the toast is considered dismissed. */\n  swipeThreshold?: number;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toast.Provider (renders no DOM element of its own). */\n  [key: string]: unknown;",
    "ToastTitle": "  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toast.Title (renders a <div>). */\n  [key: string]: unknown;",
    "ToastViewport": "  /** Keys used together with ctrl to move focus to the viewport. */\n  hotkey?: string[];\n  /** Screen-reader label for the viewport region. */\n  label?: string;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toast.Viewport (renders an <ol>). */\n  [key: string]: unknown;",
    "Toaster": "  /**\n   * Toaster takes no props of its own. It reads the toast queue from the\n   * useToast() hook (src/components/ui/use-toast.jsx) and renders each\n   * queued toast inside a ToastProvider/ToastViewport. Render it once, near\n   * the root of the app, not per-page.\n   */\n  [key: string]: unknown;",
    "Toggle": "  /** Visual style. */\n  variant?: \"default\" | \"outline\";\n  /** Size. */\n  size?: \"default\" | \"sm\" | \"lg\";\n  /** Controlled pressed state. */\n  pressed?: boolean;\n  defaultPressed?: boolean;\n  onPressedChange?: (pressed: boolean) => void;\n  disabled?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Toggle.Root (renders a <button aria-pressed>). */\n  [key: string]: unknown;",
    "ToggleGroup": "  /** Whether a single item or multiple items can be pressed at once. Required by Radix; determines the shape of value/defaultValue/onValueChange. */\n  type: \"single\" | \"multiple\";\n  /** Controlled value(s); a string when type=\"single\", a string array when type=\"multiple\". */\n  value?: string | string[];\n  defaultValue?: string | string[];\n  onValueChange?: (value: string | string[]) => void;\n  disabled?: boolean;\n  /** Whether arrow-key focus loops from last item back to first. */\n  loop?: boolean;\n  orientation?: \"horizontal\" | \"vertical\";\n  dir?: \"ltr\" | \"rtl\";\n  /**\n   * Visual style applied to every ToggleGroupItem via context, unless the\n   * item overrides it with its own variant prop. Same enum as Toggle\n   * (toggleVariants from ./toggle: \"default\" | \"outline\").\n   */\n  variant?: \"default\" | \"outline\";\n  /** Size applied to every ToggleGroupItem via context, unless overridden per item. Same enum as Toggle: \"default\" | \"sm\" | \"lg\". */\n  size?: \"default\" | \"sm\" | \"lg\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ToggleGroup.Root (renders a <div role=\"group\">). */\n  [key: string]: unknown;",
    "ToggleGroupItem": "  /** Unique value identifying this item within the group. Required. */\n  value: string;\n  disabled?: boolean;\n  /** Visual style; falls back to the parent ToggleGroup's variant (via context) when omitted. */\n  variant?: \"default\" | \"outline\";\n  /** Size; falls back to the parent ToggleGroup's size (via context) when omitted. */\n  size?: \"default\" | \"sm\" | \"lg\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix ToggleGroup.Item (renders a <button aria-pressed>). */\n  [key: string]: unknown;",
    "Tooltip": "  /** Controlled open state. */\n  open?: boolean;\n  /** Uncontrolled initial open state. */\n  defaultOpen?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Delay in ms before opening; overrides the TooltipProvider value for this tooltip. */\n  delayDuration?: number;\n  /** Whether hovering the tooltip content keeps it open. */\n  disableHoverableContent?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tooltip.Root. */\n  [key: string]: unknown;",
    "TooltipContent": "  /** Distance in px from the trigger. Defaults to 4. */\n  sideOffset?: number;\n  /** Preferred side of the trigger to render against. */\n  side?: \"top\" | \"right\" | \"bottom\" | \"left\";\n  /** Alignment against the trigger. */\n  align?: \"start\" | \"center\" | \"end\";\n  className?: string;\n  children?: React.ReactNode;\n  /** Rendered inside a Radix Portal. Remaining props pass through to Radix Tooltip.Content. */\n  [key: string]: unknown;",
    "TooltipProvider": "  /** Delay in ms before a tooltip opens on hover. Applies to every Tooltip below it. */\n  delayDuration?: number;\n  /** Window in ms during which moving to another trigger opens it instantly. */\n  skipDelayDuration?: number;\n  /** Whether hovering the tooltip content keeps it open. */\n  disableHoverableContent?: boolean;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tooltip.Provider. */\n  [key: string]: unknown;",
    "TooltipTrigger": "  /** Render the child element as the trigger instead of a <button>, merging props onto it. */\n  asChild?: boolean;\n  className?: string;\n  children?: React.ReactNode;\n  /** Remaining props pass through to the underlying Radix Tooltip.Trigger. */\n  [key: string]: unknown;"
  },
  "readmeHeader": ".design-sync/conventions.md"
}
